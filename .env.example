# Copiar a .env (ignorado por git) y definir un valor propio. No versionar credenciales reales.
# Contraseña del usuario "postgres" del Postgres de docker compose y de la API de desarrollo.
POSTGRES_PASSWORD=CAMBIAR
