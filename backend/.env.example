# Copiar a backend/.env (ignorado por git) y ajustar. No versionar credenciales reales.
# Conexión administrativa para las pruebas de integración: crean y eliminan su propia base.
BUENAONDA_TEST_CONNECTION="Host=postgres;Port=5432;Username=postgres;Password=CAMBIAR"
